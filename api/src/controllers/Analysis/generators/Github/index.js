const { generatorPipeline } = require('../../config');
module.exports = {
  generateNodeIdDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateBioDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateBlogDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateCollaboratorsDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateCompanyDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateEmailDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateFollowersDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateFollowingDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateHireableDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateLocationDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateOwnedPrivateReposDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generatePrivateGistsDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generatePublicReposDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateSiteAdminDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateTotalPrivateReposDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateTwoFactorAuthenticationDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateTypeDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateUrlDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateHtmlUrlDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateLinkDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
  generateAvatarUrlDataset(dataset, key) {
    return generatorPipeline(dataset, key);
  },
};
